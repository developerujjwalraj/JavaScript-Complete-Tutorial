"use strict"; //much stricter version of js which is used to write code accordingto newer version of JS

// alert(3+3); //This is a correct statement but since we are running JS on console or IDE instead of browser so the alert command will not work.

console.log(3+3);
console.log("Ujjwal");


let name = "Ujjwal";
let age = 22;
let isLoggedIn = false;
let state;

console.table([name, age, isLoggedIn, typeof isLoggedIn, state, typeof state]);

//Data types in JS
//Number = ranges between 2 and 2^53
//bigInt
//string = "" inside these quotes
//boolean = true/false types
//null = standalone value
//undefined = when value is not defined
//symbol = { } inside these parentheses and are unique values


console.log(typeof undefined); //undefined
console.log(typeof null); //object but its a mistake of Javascript. null is not an object it is something else on its own




